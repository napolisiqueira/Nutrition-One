spring:
  application:
    name: Nutrition-One

  datasource:
    url: jdbc:mysql://localhost:3306/nutrition_one?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: sua_senha_aqui
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

security:
  config:
    prefix: Bearer
    key: "MinhaSuperChaveSecretaParaJWT123456789012345678901234567890"
    expiration: 3600000  # 1 hora em milissegundos